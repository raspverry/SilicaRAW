---
title: Task 29.2 ICC Profile Portability
status: completed
audience: agents
updated: 2026-07-11
source_of_truth: docs/wiki/roadmaps/lut-video-service-master-plan.md
---

# Task 29.2: ICC Profile Portability

## Goal

Remove required macOS ColorSync profile-file access from `silica-export` profile lookup and `silica-render` color-probe tests while preserving ICC behavior and evidence.

## Read Before Work

- [LUT and Video Service Master Plan: Phase 29](../roadmaps/lut-video-service-master-plan.md#phase-29-service-foundation-hardening)
- [ADR 0011: Service Direction Charter](../decisions/adr-0011-service-direction-charter.md)
- `docs/09_Color_Management_Specification.md`
- [Color Management](../topics/color-management.md)
- [Task 13.3 Feature-Gated Color Profile Probe](13.3-feature-gated-color-profile-probe.md)
- [Task 13.6 ICC Export Proof](13.6-icc-export-proof.md)
- `docs/DEPENDENCIES.md`
- `crates/silica-export/src/lib.rs`
- `crates/silica-render/src/lib.rs`

## Files

- `.github/workflows/ci.yml`
- `assets/color-profiles/`
- `crates/silica-export/src/lib.rs`
- `crates/silica-render/src/lib.rs`

## Scope

- Dependency: Task 29.0 is complete, and ADR 0011 is accepted. This DAG prerequisite is satisfied.
- Replace unconditional reads from `/System/Library/ColorSync/Profiles/` with the smallest portable sRGB and Display P3 profile-byte source.
- Prefer license-compatible repository fixtures or deterministic generated profile bytes with recorded provenance and pinned SHA-256 values.
- Keep readable macOS system profiles as the preferred byte source, with portable fallback bytes; neither product lookup nor tests may require the system files.
- Make `silica-render` color-probe tests use portable profile bytes rather than macOS-only files.
- Preserve profile classification, ICC embedding, and the `icc_profile_sha256` contract. Existing macOS profile bytes and recorded profile hashes must remain unchanged.
- Require `silica-export` and feature-gated `silica-render` test evidence on both Linux and macOS through the PR CI completion gate.
- Use only existing dependencies. Do not add or change dependencies, color transforms, export defaults, or visual color-correctness claims.

## Acceptance Criteria

- `silica-export` and `silica-render` tests do not require macOS ColorSync paths.
- Portable sRGB and Display P3 profile bytes have clear license or generation provenance and pinned hashes.
- Readable macOS system-profile ICC embedding output and recorded profile SHA-256 values are unchanged.
- `icc_profile_sha256` still hashes the exact ICC payload embedded in each export.
- The PR enforces both validation commands on `macos-latest` and `ubuntu-latest` as the completion gate.
- No dependency or broad color-management behavior changes.

## Validation

Run on Linux and macOS:

```bash
cargo test -p silica-export
cargo test -p silica-render --features color-probe
scripts/harness/check.sh
git diff --check
```

## Stop Gates

Stop if Task 29.0 is not accepted, no license-clean or deterministic profile source can preserve the required semantics and hashes, macOS ICC evidence changes, a new dependency is required, or portability work expands into color transforms or correctness claims.

## Completion State

Completed on 2026-07-11.

- Bundled the CC0-1.0 `sRGB-v4.icc` and `DisplayP3Compat-v4.icc` assets from `saucecontrol/Compact-ICC-Profiles`, pinned at upstream commit `bdd84663061bc4ae95ca70decff54f581e27f702`.
- Pinned the portable profile SHA-256 values: sRGB-v4 is `c56e1685d888f5edb92fe07f2750f387f8fe8e91b32ff8fb0b56bfbbb9458353`; DisplayP3Compat-v4 is `231752984cd4a5278e1b8d2390fe496767d4511fc81f54e1a5c69ae9ab4c42b5`.
- On macOS, readable system profiles remain preferred byte-for-byte. This preserves the existing local sRGB evidence hash `2b3aa1645779a9e634744faf9b01e9102b0c9b88fd6deced7934df86b949af7e` and Display P3 evidence hash `0ff6958f98684c61f6bbdce1368ddeaf3873baf84545baba482e920d92a914c0`; a missing system profile, or any non-macOS build, uses the portable bytes.
- `silica-render` color-probe tests use the portable bytes on every platform. Product color-probe behavior is unchanged: non-macOS requests still report `UnsupportedPlatform`.
- The PR completion gate runs the exact commands below on both `macos-latest` and `ubuntu-latest`. This records the enforced gate, not a Linux result or CI run ID before CI executes.

```bash
cargo test -p silica-export
cargo test -p silica-render --features color-probe
```

- The `icc_profile_sha256` contract remains the SHA-256 of the exact ICC payload embedded in an export.
- No dependencies, color transforms, export defaults, or visual color-correctness claims changed. Task 29.3 is the default next sequential task; the Phase 29 DAG and distribution gates are unchanged.
