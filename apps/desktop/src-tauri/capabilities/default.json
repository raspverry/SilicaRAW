{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the SilicaRAW local library alpha shell.",
  "windows": [
    "main"
  ],
  "permissions": [
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
