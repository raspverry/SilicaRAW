# Temporary Icons

Phase 2 intentionally does not add final application artwork.

`placeholder-icon.svg` is a temporary source asset created for packaging validation. `icon.png` is generated from it so Tauri can complete build-time app metadata generation.

Final icons must be tracked as licensed project assets before public beta.
